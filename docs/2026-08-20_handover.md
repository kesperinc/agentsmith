# Agent Smith 타 PC 작업 이식 및 연속성 핸드오버 문서 (Handover Report)

**문서 작성일**: 2026-08-20  
**릴리즈 버전**: v1.0.0  
**현재 작업 브랜치**: `feature/setup-git-guardrails`  
**작성자**: Agent Smith AI Pair Engineering Team  
**관련 프로젝트 규칙**: AGENTS.md (규칙 14: 파이썬 uv 가상환경 자동 셋업, 규칙 16: 날짜 명명 규칙 `YYYY-MM-DD_`)

---

## 1. 개요 (Overview)

본 문서는 사용자가 현재 PC에서 작업을 마무리하고 **새로운 PC(타 개발자 장비 / 다른 개발 환경)**로 이동하여 Agent Smith 프로젝트 개발, 패키징 및 시연 작업을 연속적으로 이어서 진행할 수 있도록 작성된 **작업 연속성 핸드오버 가이드(Handover Runbook)**입니다.

---

## 2. 저장소 및 브랜치 현황 (Git Repository State)

- **원격 저장소 URI**: `https://github.com/kesperinc/agentsmith.git`
- **작업 브랜치**: `feature/setup-git-guardrails`
- **추적 예외 바이너리 (`.gitignore`)**:
  - `.venv/` (파이썬 가상환경 - 새 PC에서 생성 필요)
  - `dist/` (배포 폴더 및 installer 바이너리 - 필요 시 스크립트로 자동 생성)
  - `node_modules/`, `vscode/.build/` (빌드 용량 제외 항목)

---

## 3. 금일 핵심 결함 해결 내역 요약 (Today's Key Fixes)

1. **데스크톱 검은 화면(Black Screen) 문제 근본 원인 해결**:
   - `VSCode-win32-x64`의 29.5MB 크기 프로덕션 번들 `out/`이 7KB 개발용 스텁으로 덮어써지던 문제를 수정하여 프로덕션 번들을 온전히 보존하도록 패키징 스크립트(`scripts/package_desktop_dist.py`) 개편.
   - 28바이트 더미 asar(`{"files":{}}`)를 자동 주입하여 asar 후킹을 우회하고 Antigravity IDE의 C++ 네이티브 모듈 14종이 100% 정상 로드되도록 조치 완료.
2. **사전/사후 5초 자동 무결성 진단 도구 강화**:
   - `scripts/verify_desktop_bundle.py`에 번들 크기 정밀 검사 및 더미 asar 감지 로직 반영 완료.

---

## 4. 새로운 PC에서 작업 재개 절차 (New PC Quick-Start Runbook)

새로운 PC에서 자리를 잡은 후 아래 **5단계 명령**을 순서대로 실행하면 즉시 동기화된 환경에서 개발 및 빌드를 시작할 수 있습니다.

### Step 1: 저장소 클론 및 브랜치 이동

```powershell
# 1. 저장소 클론 (이미 클론된 경우 git pull 진행)
git clone https://github.com/kesperinc/agentsmith.git agentsmith
cd agentsmith

# 2. 작업 브랜치 체크아웃 및 최신화
git checkout feature/setup-git-guardrails
git pull origin feature/setup-git-guardrails

# 3. UTF-8 인코딩 가드레일 설정 (Windows 필수)
git config --global core.quotepath false
git config --global gui.encoding utf-8
git config --global i18n.commitencoding utf-8
```

### Step 2: Python 가상환경 (`.venv`) 구축 및 의존성 설치

```powershell
# 1. uv를 이용한 파이썬 가상환경 생성 (규칙 14 준수)
uv venv .venv

# 2. coding-agent 백엔드 필수 패키지 일괄 설치
uv pip install -r coding-agent/requirements.txt --python .venv\Scripts\python.exe
```

### Step 3: Mem0 & Qdrant 메모리DB 자동 초기화

```powershell
# 1. Mem0 설정 및 Qdrant 컬렉션 자동 셋업
powershell -ExecutionPolicy Bypass -File .\2026-08-14_setup_mem0.ps1
```

### Step 4: 빌드 환경 사전 점검 및 패키징

```powershell
# 1. 사전 환경 점검 (5초 자동 진단)
.venv\Scripts\python.exe scripts/verify_desktop_bundle.py --pre-check

# 2. 포터블 번들 및 단일 실행 인스톨러 빌드
.venv\Scripts\python.exe scripts/package_desktop_dist.py
.venv\Scripts\python.exe scripts/build_desktop_installer.py

# 3. 사후 산출물 무결성 진단
.venv\Scripts\python.exe scripts/verify_desktop_bundle.py --verify-dist
```

### Step 5: 실행 및 시연 테스트

```powershell
# 포터블 버전 즉시 실행
.\dist\agentsmith-desktop-v1.0.0\run_agentsmith_desktop.bat
```
